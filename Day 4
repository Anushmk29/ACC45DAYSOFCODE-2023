#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t,x,y;
	cin>>t;
	while(t--)
	{
	    cin>>x>>y;
	    if(x<y)
	    {
	       int sum= x+(y-x)*2;
	       cout<<sum<<endl;
	    }
	    else{
	        cout<<y<<endl;
	    }
	   
	}
	return 0;
}
